-- constraints -> These are rules applied to table columns to ensure the accuracy, validity, and integrity of the data.

use startsql;
select * from users;


-- select * from users;
-- alter table users add constraint chk_dob check (date_of_birth > '1920-01-01');
-- select * from users; 
-- alter table users drop created_at;
-- alter table users add column created_at timestamp default current_timestamp;

